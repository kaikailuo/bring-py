# 第七周 字符串处理——身份信息

## Python 基础知识

### input() 函数
- 用于获取键盘输入的字符串
- 示例：
  ```python
  a = input("请输入")
### 数据类型
- 数字：int（整型）、float（浮点型）、bool（布尔型）、complex（复数）

- 数据类型转换：可通过强制类型转换将数据从一种类型转换为另一种类型

- 示例
a = int(input("请输入"))  # 获取整数输入
a = input("请输入")
a = int(a)  # 将字符串转换为整数

### 输出函数
- 用于在屏幕上输出一个或多个输出项的值

- 多个输出项之间用逗号隔开

- 示例：

print(x)
print("你的名字:", x)
## 项目：身份证号码提取信息
- 项目情境
每位中国公民的身份证号码有18位数字

目标：从身份证号码中提取出生日期和性别信息

- 提出问题
如何提取身份证号码中的出生日期字符？

如何将提取的信息按标准格式输出？

如何判断性别？

- 算法设计（自然语言描述）
输入身份证号码 n

从 n 中切片出：y（年）、m（月）、d（日）、s（性别位）

判断性别

拼接字符串 m

输出字符串 m

分支结构：IF 语句
- 语法格式
if 条件:
    语句块1
else:
    语句块2
条件成立 → 执行语句块1

条件不成立 → 执行语句块2

## 项目拓展
1. 判断身份证号码合法性
检查输入的字符串是否为18位数字

1. 根据输入今年年份，输出年龄
2. 词频统计
简易词频统计，理解切片与循环的作用

1. 判断回文日期（如：20220202）
判断年份 y 不能小于1000，避免不合法年份

字符串切片技巧
str[0:9:2]：从0开始截取到8，每隔一个字符截取

str[::-1]：创建一个与原字符串顺序相反的字符串

str[:-5:-1]：逆序截取后4位