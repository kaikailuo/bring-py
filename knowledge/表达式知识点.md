# 构建表达式
## 关系和逻辑运算
### 关系运算符
 Python关系运算符
==  # 等于
!=  # 不等于
>   # 大于
<   # 小于
>=  # 大于等于
<=  # 小于等于

#### 示例
x = 5
y = 3
print(x > y)   # True
print(x == y)  # False

### 逻辑运算符
 Python逻辑运算符
and  # 逻辑与
or   # 逻辑或  
not  # 逻辑非

#### 真值表
"""
A     B     A and B     A or B     not A
True  True  True        True       False
True  False False       True       False
False True  False       True       True
False False False       False      True
"""
### 运算符优先级
 优先级从高到低：
1. 算术运算: (), *, /, %, +, -
2. 关系运算: >, <, >=, <=, ==, !=
3. 逻辑运算: not, and, or
### 例题
x = 3
y = -2
a = 6.5
b = -7.2

#### 计算表达式结果
result1 = x > y                    # True
result2 = x + a <= b - y           # False
result3 = x - 1 < y - b            # True
result4 = x + y > a + b and x > y  # True
result5 = x + a <= b - y or x - 1 < y - b  # True

print(f"(1) x>y: {result1}")
print(f"(2) x+a<=b-y: {result2
